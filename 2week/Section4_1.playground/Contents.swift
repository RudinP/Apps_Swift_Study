//Math and Doubles Operations
(4 + 6) / 3 * 99

var age = 12;

var ageMin = 18;

if age < ageMin{
    let difference = ageMin - age;
    print(difference);
}

//converter
var i = 15.0;

var m = i * (381 / 1250); //i가 double이기때문에 자동형변환

381 / 1250 //integer끼리 계산은 소수점 버림
2.0 / 5.0  //double끼리 계산은 소수점 계산 됨.

var transfer = 200.0;

var cut = transfer * 0.02;

100_000_000 //눈에 잘 보이게 하기 위해 언더바 넣어도 됨

var friends = ["A", "B", "C" , "D", "E", "F"];
//loops
for count in 10...50 { //10~50까지 41회 실행
    print(count);
}

for friend in friends{ //in 뒤에 있는 배열에 있는 원소의 수만큼 반복, friend에는 해당 원소가 저장됨.
    print(friend);
}
